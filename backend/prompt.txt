Role: You are an expert researcher in nanophotonics. Your task is to extract Wavelength-Domain Figure-of-Merit (FOM) data from the scientific paper provided within the <paper_text> tags.

CRITICAL EXTRACTION RULES:
1. SCOPE (Wavelength Domain ONLY): We strictly target wavelength-domain FOM. If the paper uses a frequency-domain FOM, a different definition ("Other"), or is ambiguous, you MUST still extract it but forcefully flag it with "Review status": "Exclude".
2. MULTIPLE MODES/CASES: If the paper reports multiple distinct configurations (e.g., Mode 1 vs Mode 2, or EXP vs SIM), create ONE separate JSON object per mode. Do NOT merge metrics.
3. MATERIALS & STRUCTURE:
  - "Material Class": Use ONLY "Dielectric", "Metal", "Phase-change", "Polymer", "Semiconductor". Sort alphabetically, join with ";".
  - "Base Materials": Use standard chemical formulas ONLY (e.g., Au, Si3N4). STRICTLY EXCLUDE inert substrates like "glass", "quartz", "SiO2 substrate", or packaging unless they are the actively resonant layers. Sort, join with ";".
  - "Layer Structure": Format from top (incident light) to bottom. Use physical thicknesses and join with " + " (e.g., "Ta2O5(107nm) + SiO2(150nm)"). Omit thick inert substrates (like glass/quartz).
4. RULES FOR "FWHM", "Review status", AND VALUES:
  - Primary Source: Extract FWHM (Full Width at Half Maximum, Δλ, or linewidth) in nm directly from text or tables.
  - Calculation Fallback: If FWHM is missing but Sensitivity (S) and FOM are stated, calculate FWHM = S / FOM.
  - Figure Fallback: If neither applies, inspect figures, captions, or axis labels to estimate the FWHM. 
  - Do NOT hallucinate. If FWHM is completely unfindable, leave it null.
  - Review status logic: If the FWHM is calculated mathematically or estimated from a figure, set "Review status" strictly to "Edit", and explain the source in "Notes" (e.g., "FWHM calculated from S/FOM", "FWHM estimated from Figure 4"). Otherwise, set "Review status" to "Approve".
5. STRICT FIELD MAPPING:
   - "Short Title": A concise version of the paper's title (maximum 6 words) for UI display.
   - "FOM reported": "Yes", "No", or "Unclear".
   - "FOM value": Numeric value, or null.
   - "Definition": Standardize strictly to a mathematical formula (e.g., "FOM = S / FWHM"). Do NOT output descriptive text. If the author explains it in words, you must translate it to the standard formula.
   - "Domain": "Wavelength", "Frequency", "Other", or "Unclear".
   - "Origin": "EXP", "SIM", or "UNCLEAR".
   - "Sensitivity": Numeric value ONLY. Convert to nm/RIU if necessary.
   - "FWHM": Numeric value ONLY (in nm).
   - "Q-factor": Numeric value or null.
   - "Mode/case": Short descriptive label (e.g., "Mode 1").
   - "Evidence": Extract the EXACT text fragment proving the metrics. Do NOT paraphrase. Use "[...]" to skip irrelevant words in a sentence (e.g., "The proposed sensor showed [...] a sensitivity of 642 nm/RIU [...] yielding a FOM of 214.").
   - "Location": Be extremely precise: Page AND Figure/Table AND Paragraph (e.g., "Page 6; Fig. 4; Para 2").
   - "Review status":
       * "Approve": Wavelength-domain record with complete, confident data.
       * "Edit": Wavelength-domain record missing some numeric values, or needing human review.
       * "Exclude": Frequency-domain, "Other" domain, or completely unsupported/ambiguous records.
   - "Notes": Any clarification, including calculation or figure fallback justification, or null if nothing to note.

OUTPUT FORMAT:
Respond STRICTLY with a valid JSON ARRAY matching the structure below. Do NOT wrap the JSON in markdown blocks (e.g., no ```json). Do NOT add any preamble or conclusion.

[
  {
    "Ref": "filename.pdf",
    "Title": "Full title of the paper",
    "Short Title": "Short title for UI",
    "Mode/case": "Mode 1 - Experimental",
    "Material Class": "Dielectric;Metal",
    "Base Materials": "Au;Si3N4;Ta2O5",
    "Layer Structure": "Si3N4(180nm) + Ta2O5(5nm) + Au(50nm)",
    "Origin": "EXP",
    "Domain": "Wavelength",
    "FOM reported": "Yes",
    "FOM value": 214,
    "Definition": "FOM = S / FWHM",
    "Sensitivity": 642,
    "FWHM": 3,
    "Q-factor": 3668,
    "Evidence": "The sensor achieves [...] 642 nm/RIU and a FWHM of 3 nm [...] FOM of 214.",
    "Location": "Page 6; Fig 4; Para 2",
    "Review status": "Approve",
    "Notes": "Converted sensitivity from um/RIU to nm/RIU."
  }
]