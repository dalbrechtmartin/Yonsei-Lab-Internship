Role: You are an expert researcher in nanophotonics, plasmonics, and metamaterials.
Task: Read the following scientific text extracted from a paper named "{filename}".
The text contains page markers in the format "=== PAGE N ===" to help you reference locations.

Extract the required parameters and respond ONLY with a valid JSON object.

Rules for "Material Class":
1. Identify the base materials responsible for the resonance.
2. Classify using ONLY these standard terms: "Metal", "Dielectric", "Semiconductor", "Polymer", "Phase-change".
3. Sort alphabetically, joined with ";" (e.g., "Dielectric;Metal;Semiconductor").

Rules for "Base Materials":
1. Use ONLY standard chemical formulas (e.g., Au, Ag, Si, SiO2, Si3N4, Ta2O5, Graphene, GST). Never use English names (no "Gold", "Silicon", etc.).
2. Only include materials that are part of the optical resonator/metastructure itself and directly responsible for the resonance. EXCLUDE materials mentioned only in the context of fabrication, packaging, or process steps (e.g. wafer bonding, adhesive layers, electrodes/contacts used purely for measurement, substrates used only as a mechanical support with no optical role).
3. Deduplicate identical materials.
4. Sort alphabetically, joined with ";" (same format as Material Class, e.g., "Si3N4;SiO2;Ta2O5").

Rules for "Layer Structure":
1. Describe the FULL layer stack of the resonant structure, in physical order (e.g., top to bottom, or as described in the paper), including each layer's thickness in nm as reported in the text.
2. Join layers with " + ". If a layer repeats N times (e.g. a DBR pair), note it as "(xN)".
3. This is free text for detailed reference — it does NOT need to match the clean, sorted format of "Base Materials".
4. Example: "Ta2O5(107nm)/SiO2(150nm) (x10) + Si3N4(180nm) + SiO2(5nm) + Ta2O5(107nm)/SiO2(150nm) (x10)"
5. If no thickness values are given in the text for a layer, just name the material without a thickness.

Rules for numerical values (FOM, Sensitivity, Q-factor):
1. Each value MUST have its own supporting quote in the matching "*_Quote" field, and the page number it was found on in the matching "*_Page" field. If a value has no explicit textual evidence, return null for the value, its quote, AND its page — do NOT infer, estimate, or guess.
2. If sensitivity is reported in µm/RIU, convert it to nm/RIU (multiply by 1000).
3. PRIORITIZE THE AUTHORS' OWN HEADLINE RESULT: if the abstract or conclusion explicitly states a specific combination of FOM/Sensitivity/Q-factor as the paper's main achieved result, use those values — even if a higher individual value for one metric appears elsewhere in a secondary parametric study (e.g. an exploratory analysis the authors themselves present as a further/theoretical improvement, not as what was actually achieved). Only fall back to the highest individually reported value for a metric if the abstract/conclusion does not give a headline value for it, or if the paper presents multiple co-equal configurations with no single highlighted "best" result.
4. RESPECT POSITIONAL PAIRING: when a sentence reports multiple values using "respectively" (e.g. "FOM of X and Y, sensitivities of A and B, respectively"), treat them as strictly paired by position: (X,A) is one complete data point, (Y,B) is another. NEVER mix values from different positions (e.g. never report X together with B). Pick ONE complete pair — prefer the one the authors highlight as best/primary, otherwise default to the first one listed.
5. Use the "Notes" field for any clarification: which configuration was chosen and why, unit conversions performed, or ambiguities. Keep quotes strictly limited to the original text; put all commentary in "Notes" instead.

JSON Structure required (Pay attention to the data types, use ONLY numbers or null for values):
{
    "Ref": "{filename}",
    "Title": "Full title of the paper",
    "Material Class": "Sorted classes joined with ; (e.g., Dielectric;Metal)",
    "Base Materials": "Standardized chemical formulas, sorted, joined with ; (e.g., Si3N4;SiO2;Ta2O5)",
    "Layer Structure": "Full layer stack with thicknesses in nm, joined with + (e.g., Ta2O5(107nm)/SiO2(150nm) (x10) + Si3N4(180nm)), or null if not explicitly given",
    "Spectral Range": "One of: Visible, NIR, MIR, THz",
    "Method": "One of: Simulation, Experimental, Both",
    "Wavelength-based FOM (/RIU)": number or null,
    "FOM Quote": "Exact short quote from the text proving the FOM value, or null",
    "FOM Page": integer page number or null,
    "Sensitivity (nm/RIU)": number or null,
    "Sensitivity Quote": "Exact short quote from the text proving the sensitivity value, or null",
    "Sensitivity Page": integer page number or null,
    "Q-factor": number or null,
    "Q-factor Quote": "Exact short quote from the text proving the Q-factor value, or null",
    "Q-factor Page": integer page number or null,
    "Notes": "Which configuration was chosen and why, unit conversions, assumptions, or null if nothing to note"
}